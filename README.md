# CS230 Project

### Installation

```shell
pip install -r requirements.txt
```

Maybe use `pip3` instead of `pip`.

### Python Version

> Python version >= python3.6

### Example

```shell
cd src
python link_pred.py
```

